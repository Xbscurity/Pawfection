@model List<Pet>

<h1>Search Results</h1>

@if (Model.Count == 0)
{
    <p>No results found.</p>
}
else
{
    <ul>
        @foreach (var pet in Model)
        {
            <li>@pet.Name (Breed: @pet.Breed.Name)</li>
        }
    </ul>
}
